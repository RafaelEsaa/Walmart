import { makeStyles } from '@material-ui/core/styles'

export const useStyles = makeStyles((theme) => ({
    discount: {
        backgroundColor: 'red',
        borderRadius: 100,
        fontSize: 11,
        padding: '2px 5px',
        color: 'white',
        fontWeight: 700,
        marginLeft: 5
    },
}))